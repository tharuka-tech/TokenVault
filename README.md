TokenVault System is a full-stack project built with ASP.NET Core for the backend and React for the frontend. The backend provides secure REST APIs with JWT authentication (access and refresh tokens), while the React frontend interacts with these APIs to provide a user-friendly interface. The system includes login functionality, a dashboard displaying user-specific data, and can be hosted locally or on IIS for both backend and frontend.
